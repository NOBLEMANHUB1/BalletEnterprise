{
    "configurations": [
        {
            "type": "chrome",
            "request": "launch",
            "name": "Debug localhost",
            "url": "http://localhost:2261undefined",
            "webRoot": "${workspaceFolder}"
        }
    ]
}